Use `AGENTS.md` as the canonical editing guide for this repo.

When working in this repository:

- prefer presets and centralized content/config edits first
- keep the structure reusable and content-driven
- preserve static-first performance
- keep metadata, schema, `robots.txt`, and `llms.txt` accurate
- maintain answer-first page structure
- finish with `npm run lint`, `npm run check`, and `npm run build`
